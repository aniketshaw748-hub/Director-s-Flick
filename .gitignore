# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
node_modules/
dist/
dist-ssr/
*.local

# Node.js
app/node_modules/
ui/node_modules/
node_modules/

# OS/IDE
.DS_Store
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Databases
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Vite / React Build output
ui/dist/
ui/build/

# Backend Data / Generated assets
app/projects/**/*.png
app/projects/**/*.mp4
app/projects/**/*.db
app/projects/**/*.db-shm
app/projects/**/*.db-wal
app/projects/**/*.json

# Accounts / credentials
app/accounts/

# Python (if virtual environments exist)
venv/
.venv/
__pycache__/
*.pyc

# Docs
prd.md

# --- Fable repo hygiene (2026-07-03) ---
# Generated runtime projects (SQLite, downloaded media, exports) — recreate via CLI
app/projects/
# ffmpeg smoke-test intermediates + analysis/verification frames (derivable)
phase0/seg*.mp4
phase0/timeline_silent.mp4
phase0/export_test.mp4
phase0/frame_*.png
phase0/T08_*.png
phase0/T33_*.png
# vitest coverage output (T-53 audit runs)
app/coverage/
# T-86: unreferenced Phase-0 test voiceover (kept locally as evidence, untracked)
phase0/t08_vo.wav
# T-84: scratch dir for in-flight multipart uploads (streamed to disk, moved
# into the real project dir or deleted on validation failure - nothing here
# should ever be intentionally long-lived)
app/tmp-uploads/
