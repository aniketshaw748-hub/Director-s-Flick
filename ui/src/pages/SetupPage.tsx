export default function SetupPage({}: { shots: any[] }) {
  return (
    <main className="content">
      <div className="page-head">
        <h1>Project setup</h1>
        <p>Align the script to the voiceover, lock the cast, start the run.</p>
      </div>
      <div>Setup page content...</div>
    </main>
  );
}
